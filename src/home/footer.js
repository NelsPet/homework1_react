

export const Footer = (props) => {
    return (
        <div style={{display:props?.display, justifyContent:props?.justifyContent}}>
            <textarea style={{width:props?.width}}>
            </textarea>
        </div>
    )
}